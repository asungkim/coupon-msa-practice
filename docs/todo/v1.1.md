# TODO v1.1 (Requirement v1.0 → Spec v1.0 기준)

> 요구사항 출처: `docs/requirement/v1.0.md`
> 스펙 반영: `docs/spec/v1.0.md`
> 세부 기능은 각 TODO 진행 시 PLAN 문서에서 정의한다.

# Phase 1. 기본 기능 구현 ✅

## 🔹 Epic: 엔티티 및 기반 구조 ✅

- ✅ User 엔티티 + Repository
- ✅ Coupon 엔티티 + Repository
- ✅ CouponIssue 엔티티 + Repository + IssueStatus Enum
- ✅ 예외 클래스 정의 (UserNotFound, CouponNotFound, CouponAlreadyIssued, CouponOutOfStock, CouponNotAvailable, InsufficientPoints)
- ✅ 글로벌 예외 핸들러

## 🔹 Epic: 셋업 API ✅

- ✅ POST `/api/users` — 유저 생성 (Controller, UserService)
- ✅ POST `/api/users/{userId}/points` — 포인트 충전 (Controller, UserService)
- ✅ PointService — 포인트 차감 로직 (쿠폰 발급 시 CouponService에서 호출)
- ✅ POST `/api/coupons` — 쿠폰 생성 (Controller, CouponService)

## 🔹 Epic: 쿠폰 발급 ✅

- ✅ CouponService — 쿠폰 발급 로직 (검증 → PointService.decreasePoints → 재고 차감 → 발급 이력 저장)
- ✅ POST `/api/coupons/{couponId}/issue` — 발급 API (Controller)
- ✅ PushNotificationClient — 알림 서버 연동 (트랜잭션 내 동기 호출)

## 🔹 Epic: 테스트 ✅

- ✅ 단위 테스트 — 44개 작성 완료 (각 Epic에서 구현)
- ✅ 통합 테스트 (발급 API 정상/실패 4케이스, @SpringBootTest)
- ✅ 부하 테스트 (CouponLoadTest — 300명/200쿠폰, Phase 1 문제 확인완료)

---

# Phase 2. 문제 해결 (외부 인프라 없이, DB + 애플리케이션 수준)

## 🔹 Epic: 알림 분리

> 문제: 트랜잭션 내 알림 동기 호출(2~3초) → 커넥션 장시간 점유 → HikariCP 10개 고갈

### 선행: 부하 테스트 기반 정비
- ✅ 조회 API 추가 (GET `/api/coupons/{couponId}`, 발급 건수)
- ✅ 부하 테스트 리팩토링 (DB 정합성 검증 + 결과 파일 저장)
- ✅ Phase 1 baseline 결과 기록

### Step 1: 트랜잭션 분리 + 동기 호출
- ✅ 이벤트 기반 분리 (`@TransactionalEventListener(AFTER_COMMIT)`)
- ✅ PushNotificationClient 인터페이스 + Sync/Async 구현체 분리
- ✅ 부하 테스트 + 결과 기록 (Step 1: 커넥션 회전 개선, lost update 여전)

### Step 2: 비동기 전환
- ✅ notification.client.mode=async 전환
- ✅ 부하 테스트 + 결과 기록 (Step 2: 0.5초/300명 전원 성공, 커넥션 고갈 해소)
- ✅ Phase 1 / Step 1 / Step 2 성능 비교 — `docs/history/load_test_results.md`

## 🔹 Epic: 재고 동시성

> 문제: Java 레벨 `remainingQuantity--` → 2대 서버 환경에서 lost update 발생
> 제약: 서버 N대, DB 1대 — DB가 유일한 동기화 지점

- ✅ 전략 패턴 구현 (비관적 락 / 원자적 UPDATE, 설정으로 전환)
- ✅ UK 위반 예외 처리 (DataIntegrityViolationException → 409)
- ✅ 300명 부하 테스트: 비관적(0.8초) / 원자적(0.6초) 둘 다 정합성 PASS
- ✅ 500명 확장 테스트: 비관적(0.9초, 안정) / 원자적(16초, H2 특성 의심)
- ✅ 결과 `docs/history/load_test_results.md`에 누적 기록

## 🔹 Epic: 부하 테스트 검증 강화

- ✅ 조회 API 추가 (GET /api/coupons/{couponId}, 발급 건수)
- ✅ 부하 테스트 정합성 검증 보강 (remainingQuantity, 발급건수, 소비량 비교)

## 🔹 Epic: Phase 2 최종 검증

- ✅ 부하 테스트 전체 통과 (정합성 PASS)
- ✅ Phase 1 → Phase 2 개선 결과 비교 — `docs/history/load_test_results.md`

---

# Phase 2.5. Spring Cloud Gateway 로드밸런싱

## 🔹 Epic: Gateway 모듈 구축

> 목표: 2대 서버(8080, 8081)로의 요청 분산 + 단일 vs 분산 성능 비교

- ⚪ Gateway 모듈 셋업 (Spring Cloud Gateway + 정적 인스턴스 로드밸런싱)
- ⚪ GatewayLoadTest 작성 (Gateway 분산 전용, 기존 CouponLoadTest는 단일 전용 유지)
- ⚪ 2대 서버 분산 부하 테스트 실행 + 단일 vs 분산 성능 비교 기록

---

# Phase 3. 외부 인프라 활용

> Phase 2에서 DB 수준으로 해결한 한계를 외부 인프라로 개선 (추후 설계)
